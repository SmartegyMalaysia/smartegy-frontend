import { BrandLogo } from "./brand-logo";

export function AuthShell({ kicker, title, description, children }: { kicker: string; title: string; description: string; children: React.ReactNode }) {
  return <main className="login-page"><section className="login-intro" aria-labelledby="auth-intro-title"><div className="login-intro-inner"><BrandLogo className="login-brand" variant="horizontal-tagline"/><div className="intro-copy"><p className="login-kicker">Operations workspace</p><h1 id="auth-intro-title">Keep your Smartegy workspace secure.</h1><p>Recover access safely and get back to managing cases, agents, payments, and commissions.</p></div><p className="login-footnote">Built for Smartegy agents and operations teams.</p></div></section><section className="login-form-side" aria-labelledby="auth-title"><div className="login-card"><div className="login-card-heading"><p className="login-kicker">{kicker}</p><h2 id="auth-title">{title}</h2><p>{description}</p></div>{children}</div><p className="login-legal">© 2026 Smartegy · Your operational workspace</p></section></main>;
}
